# Sage 4/3 versus GNU 3/2 public benchmark campaign

This orphan branch is the clean-history public execution authority for the ten-pair mirrored seven-point Sage versus GNU benchmark campaign.

Campaign settings are committed in `experiments/sage-gnu-campaign-v1/campaign.json`. The runner consumes those settings and creates machine-local runtime/artifact folders; the folders are execution state, not experiment authority.

This snapshot derives from private commissioning commit `6c8771c93f0d0416068bebc5d308601a515c47a9` in `backgammonsimplified/backgammon-engine-benchmarker`. Unrelated retained Stage 1 sources, Canonical ingestion code, and private operational history are excluded.

Engine Kit authority is public tag `v0.4.0`, tag-target commit `f13446140ea06f9dc1ef51d4b6b0c83c5a46237d`, with wheel/sdist/dependency-lock hashes committed under `release-assets/v0.4.0/` on the public release branch.

No match compute is performed by the publication operator.
