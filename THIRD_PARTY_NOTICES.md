# Third-Party Notices

Document all engines, binaries, models, weights, generated engine output, libraries, and external data used by benchmark releases.

For each dependency or release input record:

```text
name and version
source
licence/terms
configuration/model version
redistributed files
runtime-only or bundled
release-specific caveat
```

Benchmark licences apply only to rights the project can legally grant.
